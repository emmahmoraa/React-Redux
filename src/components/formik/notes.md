- Helper functions in formik include:

  1. **handleSUbmit** : A submission handler.
  2. **handleChange** : A change handler to pass to each `<input>`, `<select>`, or `<textarea>`
  3. **values** : Our form's current values.
